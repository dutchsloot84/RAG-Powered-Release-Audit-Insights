version: '3.9'
services:
  app:
    build: .
    volumes:
      - ./cache:/app/cache
      - ./corp.pem:/app/certs/corp.pem:ro
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
