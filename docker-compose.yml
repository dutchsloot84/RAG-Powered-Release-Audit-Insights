version: '3.9'
services:
  app:
    build: .
    volumes:
      - ./cache:/app/cache
      - ./corp.pem:/app/certs/corp.pem:ro
    environment:
      - PYTHONUNBUFFERED=1
      - PEM_PATH=/app/certs/corp.pem
    ports:
      - "8501:8501"
